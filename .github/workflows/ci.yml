name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    uses: ./.github/workflows/quality.yml
    with:
      source_sha: ${{ github.sha }}
      produce_pages_artifact: false
    permissions:
      contents: read
